get:
  tags:
    - project
  summary: Meta Models List
  description: Retrieves the list of available meta models.
  operationId: getMetaModels
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../models/metaModel.yml#/MetaModel"
