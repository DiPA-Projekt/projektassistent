MetaModel:
  type: object
  properties:
    id:
      description: Unique identifier of the meta model
      type: integer
      format: int64
      example: 1
    name:
      description: Name of the meta model
      type: string
      example: "VMXT ITZ Bund"
    description:
      description: Short description text for the meta model
      type: string
      example: "V-Modell XT des ITZ Bund"
  required:
    - id
    - name
